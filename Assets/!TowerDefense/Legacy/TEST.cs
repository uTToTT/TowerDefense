using UnityEngine;

public class TEST : MonoBehaviour
{
    private void Awake()
    {
        //for (int i = 1; i <= SceneManager.sceneCountInBuildSettings; i++)
        //{
        //    PlayerPrefs.SetInt("StarsForLevel" + i, 3);
        //    Debug.Log(PlayerPrefs.GetInt("StarsForLevel" + i));
        //}

        //int _starCounter = 0;

        //for (int i = 0; i < SceneManager.sceneCountInBuildSettings; i++)
        //{
        //    _starCounter += PlayerPrefs.GetInt("StarsForLevel" + (i + 1));
        //}

        //PlayerPrefs.SetInt("StarCount", _starCounter);

        //Debug.Log(PlayerPrefs.GetInt("StarsForLevel" + 50));
        //PlayerPrefs.SetInt("RemoveAds", 0);
    }
}
